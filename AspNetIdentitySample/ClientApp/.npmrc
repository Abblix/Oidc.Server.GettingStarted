# Some tooling (the Tailwind and Vite plugins) still declares conservative peer ranges that have not
# caught up to the Vite 8 / TypeScript 7 versions this app pins. The combinations work in practice, so
# relax peer strictness rather than hold the app back on older majors.
legacy-peer-deps=true
